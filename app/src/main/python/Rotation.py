import numpy as np
import cv2
import base64

i = 0
def rotation_func(i1, i2, i3, i4, i5, i6, i7, i8, i9, i10, i11, i12, i13, i14, i15, i16, i17, i18, i19, i20):
    # decoded_data = base64.b64decode(data)
    # np_data = np.fromstring(decoded_data, np.uint8)
    # img = cv2.imdecode(np_data, cv2.IMREAD_UNCHANGED)
    # cv2.imwrite('.', img)

    # cv2.imshow("capture", img)
    # cv2.waitKey(0)
    # cv2.destroyAllWindows()
    # global i
    # i += 1
    print("#################")

    # print(np.asarray(data).shape)


    # image = cv2.imdecode(np.asarray(data), cv2.IMREAD_COLOR)
    # print(image.shape)
    # rotate = cv2.rotate(image, cv2.ROTATE_180)
    # _, im_buf_arr = cv2.imencode(".jpeg", rotate)
    #
    #
    # byte_im = im_buf_arr.tobytes()
    # print(len(byte_im))

    # return data
    # return byte_im